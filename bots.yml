version: '3.5'
services:
  backupbot:
    image: femiwiki/backupbot:build-5
    env_file:
      - configs/bot-secret.env
    networks:
      hostnet: {}

networks:
  hostnet:
    external: true
    name: host
